<ui:FluentWindow x:Class="AsaHookCreator.GeneratedCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Generated Hook Code"
        Height="500" Width="800"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Grid Background="#0D0D0D">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Generated Hook Code" 
                     ShowMaximize="True" 
                     ShowMinimize="True"
                     Background="#151515" />

        <!-- Code Content -->
        <Border Grid.Row="1" 
                Background="#1E1E1E" 
                Margin="16,8,16,8"
                CornerRadius="12"
                BorderBrush="#333333"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <!-- Header with Orange accent -->
                <Border Grid.Row="0" 
                        Background="#252526" 
                        CornerRadius="12,12,0,0"
                        Padding="12,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Orientation="Horizontal">
                            <Border CornerRadius="6" Padding="6" Margin="0,0,10,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF8C00" Offset="0"/>
                                        <GradientStop Color="#FFA500" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ui:SymbolIcon Symbol="Code24" FontSize="12" Foreground="White" />
                            </Border>
                            <TextBlock x:Name="TitleText"
                                       Text="Hook Code" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ui:Button x:Name="CopyButton"
                                       Content="Copy"
                                       Icon="{ui:SymbolIcon Copy24}"
                                       Appearance="Primary"
                                       Click="CopyButton_Click"
                                       Padding="12,6"
                                       Margin="0,0,8,0" />
                            <ui:Button x:Name="SaveButton"
                                       Content="Save"
                                       Icon="{ui:SymbolIcon Save24}"
                                       Appearance="Secondary"
                                       Padding="12,6"
                                       Click="SaveButton_Click" />
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Code Display with RichTextBox for better display -->
                <RichTextBox x:Name="CodeRichTextBox"
                             Grid.Row="1"
                             FontFamily="Cascadia Code, JetBrains Mono, Consolas, Courier New"
                             FontSize="13"
                             IsReadOnly="True"
                             Background="#1E1E1E"
                             Foreground="#D4D4D4"
                             BorderThickness="0"
                             Padding="16,12"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto">
                    <RichTextBox.Resources>
                        <Style TargetType="Paragraph">
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="LineHeight" Value="20"/>
                        </Style>
                    </RichTextBox.Resources>
                </RichTextBox>
            </Grid>
        </Border>

        <!-- Bottom Bar -->
        <Border Grid.Row="2" 
                Background="#151515"
                BorderBrush="#2D2D2D"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Checkmark24" Foreground="#4EC9B0" Margin="0,0,10,0" />
                    <TextBlock x:Name="StatusText"
                               Text="Ready to copy"
                               VerticalAlignment="Center"
                               Foreground="#888888"
                               FontSize="12" />
                </StackPanel>

                <ui:Button Grid.Column="1"
                           Content="Close"
                           Click="CloseButton_Click"
                           Padding="16,6"
                           Appearance="Secondary" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>

